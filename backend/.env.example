# Backend Environment Variables (Local Development Only)
# Copy this file to .env and fill in your actual values
#
# For production/Lambda deployment:
# - Sensitive values are stored in AWS Secrets Manager
# - Secret name is configured in config.py: "financial-pipeline/secrets"
# - Non-sensitive config (S3 bucket name, region) is in config.py

# Polygon.io API Key (get from https://polygon.io)
# In production: stored in AWS Secrets Manager as POLYGON_API_KEY
POLYGON_API_KEY=your_polygon_api_key_here

# Note: S3_BUCKET_NAME is now configured in config.py (not environment variables)
